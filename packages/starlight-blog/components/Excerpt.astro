---
import { Markdown } from 'astro-remote'

import { getBlogEntryExcerpt, type StarlightBlogEntry } from '../libs/content'

import ExcerptContent from 'virtual:starlight-blog/components/ExcerptContent'

interface Props {
  entry: StarlightBlogEntry
}

const { entry } = Astro.props

const Content = await getBlogEntryExcerpt(entry)
---

{
  typeof Content === 'string' ? (
    <Markdown content={Content} />
  ) : (
    <ExcerptContent>
      <Content />
    </ExcerptContent>
  )
}

---
import { getBlogEntries } from '../libs/content'

const blogEntries = await getBlogEntries()

// TODO(HiDeoo) hide title ?
---

<div>
  {blogEntries.map((blogEntry) => <div>{blogEntry.data.title}</div>)}
</div>

---
import Page from './_Page.astro'
import { getBlogStaticPaths } from '../libs/content'

export function getStaticPaths() {
  return getBlogStaticPaths()
}

// TODO(HiDeoo) hide title ? CSS only in this page?
// TODO(HiDeoo) hide toc

// TODO(HiDeoo) review all this
const entry = {
  data: {
    head: [],
    tableOfContents: false,
    title: 'Landing',
  },
}
---

<Page entry={entry} lang="en" entryMeta={{ lang: 'en' }}>
  <div>LANDING</div>
</Page>
